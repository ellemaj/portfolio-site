APP_ENV=""
VIEWS_PATH="app/views"
APP_URL=""
DB_HOST=
DB_NAME=
DB_USER=
DB_PASS=

# Address that receives contact form submissions
CONTACT_EMAIL=

# Only used by docker-compose.yml for the local dev MySQL container
DOCKER_DB_PASSWORD=changeme